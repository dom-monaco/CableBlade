<h1>Select your Channels</h1>
<p>Choose the Channels that you watch, then click submit.  Keep in mind that sister channels, such as Disney XD, History2, Nick Jr, etc. have been grouped under the main channel name.  For example if you watch Disney XD, you would choose Disney.</p>

<%= form_tag(shows_path, method: 'get') do %>
  <% @networks.each do |network| %>
		<div class="col-xs-6 col-md-3">
      <label>
        <%= image_tag network.image, class: "center-block" %>
        <%= check_box_tag('networks[]', network.id) %>
      </label>
	  </div>
  <% end %>

  <div class="col-xs-12"><input type="submit" value="Next"></div>
<% end %>
